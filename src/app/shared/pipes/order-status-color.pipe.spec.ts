import { OrderStatusColorPipe } from './order-status-color.pipe';

describe('OrderStatusColorPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderStatusColorPipe();
    expect(pipe).toBeTruthy();
  });
});
